mod lore;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .plugin(tauri_plugin_dialog::init())
        .invoke_handler(tauri::generate_handler![
            lore::lore_status,
            lore::lore_stage,
            lore::lore_commit,
            lore::lore_push,
            lore::lore_sync,
            lore::lore_branches,
            lore::lore_switch_branch,
            lore::lore_create_branch,
            lore::lore_diff,
            lore::lore_merge_branch,
            lore::lore_merge_start,
            lore::lore_merge_resolve,
            lore::lore_merge_finish,
            lore::lore_merge_abort,
            lore::lore_create_repo,
            lore::lore_clone,
            lore::ensure_unreal_setup,
            lore::lore_history,
            lore::lore_history_from,
            lore::lore_discard,
            lore::lore_commit_files,
            lore::lore_commit_file_diff,
            lore::lore_revert_commit,
            lore::lore_undo_commit,
            lore::lore_run,
            lore::lore_set_globals,
            lore::lore_server_status,
            lore::lore_start_server,
            lore::default_server_data_dir,
            lore::lore_stop_server,
            lore::lore_is_repo,
            lore::file_meta,
            lore::read_file_bytes,
            lore::read_uasset_thumb,
            lore::read_blend_thumb,
            lore::read_blend_info,
            lore::blend_to_glb,
            lore::blend_is_cached,
            lore::reveal_in_explorer,
            lore::write_server_store_config,
            lore::lore_identity,
            lore::lore_set_identity,
            lore::lore_repo_remote,
            lore::start_repo_watch,
            lore::local_ip,
            lore::tailscale_ip,
            lore::lore_revision_sync,
            lore::lore_revision_cherry_pick,
            lore::lore_revision_amend,
            lore::lore_revision_info,
            lore::lore_branch_reset,
            lore::lore_branch_create_at,
            lore::lore_branch_push,
            lore::lore_branch_info,
            lore::lore_branch_archive,
            lore::lore_branch_protect,
            lore::lore_lock_acquire,
            lore::lore_lock_release,
            lore::lore_lock_status,
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
